spring:
  application:
    name: icecreamshop
  datasource:
    url: jdbc:postgresql://localhost:5433/icecream
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
